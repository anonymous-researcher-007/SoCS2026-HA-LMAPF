# Sweep: task_allocator
# Compares different task allocation strategies

sweep_param: task_allocator
sweep_values: ["greedy", "hungarian", "auction"]

# Base config to inherit
base_config: "configs/tuning/base.yaml"
